import { DashboardLoading } from './dashboard-client';

/** Esqueleto del panel mientras el servidor resuelve empresa, reseñas y cuota. */
export default function Loading() {
  return <DashboardLoading />;
}
